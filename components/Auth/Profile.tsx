import { View, Text, Button } from "react-native";

export default function Profile({ profileName, setProfileame}) {

    return (
        <View>
            <Text>Det här är profilsidan.</Text>
        </View>
    );
};