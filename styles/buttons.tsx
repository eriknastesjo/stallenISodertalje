export const buttonContainer = {
    // overflow: 'hidden',
    // paddingBottom: 5,
}

export const button = {
    width: 160,
    height: 160,
    justifyContent: "center",
    backgroundColor: '#FAFAFA',
    borderRadius: 8,
    padding: 10,
    marginBottom: 20,
    marginLeft: 8,
    marginRight: 8,

    // shadowColor: '#006BFF',
    // shadowOffset: { width: 150, height: 150 },
    // shadowRadius: 200,
    // shadowOpacity: 1,

    shadowOffset: { width: 0, height: 1 },
    shadowColor: 'black',
    shadowOpacity: 0.3,
    elevation: 4,
    shadowRadius: 2,
    // background color must be set
    // backgroundColor: "#0000" // invisible color
    zIndex: 999,
}

export const buttonGrid = {
    alignSelf: 'center',
    flex: 1,
    justifyContent: "center",
    width: 170,
    height: 170,
    backgroundColor: '#FAFAFA',
    borderRadius: 8,
    padding: 10,
    marginBottom: 10,
    // marginLeft: 8,
    // marginRight: 8,

    // shadowColor: '#006BFF',
    // shadowOffset: { width: 150, height: 150 },
    // shadowRadius: 200,
    // shadowOpacity: 1,

    shadowOffset: { width: 0, height: 1 },
    shadowColor: 'black',
    shadowOpacity: 0.3,
    elevation: 4,
    shadowRadius: 2,
    // background color must be set
    // backgroundColor: "#0000" // invisible color
    zIndex: 999,
}

export const button2 = {
    backgroundColor: '#398440',
    // backgroundColor: '#FAFAFA',
    borderRadius: 8,
    padding: 12,
    marginBottom: 20,
    marginLeft: 20,
    marginRight: 20,

    // shadowColor: '#006BFF',
    // shadowOffset: { width: 150, height: 150 },
    // shadowRadius: 200,
    // shadowOpacity: 1,

    shadowOffset: { width: 0, height: 1 },
    shadowColor: 'black',
    shadowOpacity: 0.3,
    elevation: 4,
    shadowRadius: 2,
    // background color must be set
    // backgroundColor: "#0000" // invisible color
    zIndex: 999,
    flexDirection: "row",
}

export const buttonCenter = {
    backgroundColor: '#398440',
    // backgroundColor: '#FAFAFA',
    borderRadius: 8,
    padding: 10,
    marginBottom: 20,
    marginLeft: 20,
    marginRight: 20,

    // shadowColor: '#006BFF',
    // shadowOffset: { width: 150, height: 150 },
    // shadowRadius: 200,
    // shadowOpacity: 1,

    shadowOffset: { width: 0, height: 1 },
    shadowColor: 'black',
    shadowOpacity: 0.3,
    elevation: 4,
    shadowRadius: 2,
    // background color must be set
    // backgroundColor: "#0000" // invisible color
    zIndex: 999,
}

export const buttonInRightCorner = {
    backgroundColor: '#398440',
    // backgroundColor: '#FAFAFA',
    position: 'absolute',
    bottom: 45,
    right: 12,

    // borderRadius: 8,
    padding: 3,
    paddingTop: 4,
    paddingBottom: 4,
    marginBottom: 20,
    // marginLeft: 40,
    // marginRight: 40,
    width: 95,
    alignSelf: 'center',

    // shadowColor: '#006BFF',
    // shadowOffset: { width: 150, height: 150 },
    // shadowRadius: 200,
    // shadowOpacity: 1,

    shadowOffset: { width: 0, height: 1 },
    shadowColor: 'black',
    shadowOpacity: 0.3,
    elevation: 4,
    shadowRadius: 1,
    // background color must be set
    // backgroundColor: "#0000" // invisible color
    zIndex: 999,
}

export const gridContainer = {
    marginLeft: 10,
    marginRight: 10,

}

export const buttonProfile = {
    backgroundColor: '#46A450',
    borderRadius: 8,
    padding: 14,
    marginBottom: 20,
    marginLeft: 20,
    marginRight: 20,

    // shadowColor: '#006BFF',
    // shadowOffset: { width: 150, height: 150 },
    // shadowRadius: 200,
    // shadowOpacity: 1,

    shadowOffset: { width: 0, height: 1 },
    shadowColor: 'black',
    shadowOpacity: 0.3,
    elevation: 4,
    shadowRadius: 2,
    // background color must be set
    // backgroundColor: "#0000" // invisible color
    zIndex: 999,
    flexDirection: "row",
    alignItems: "center",
}

export const buttonProfileSmall = {
    backgroundColor: '#46A450',
    borderRadius: 8,
    padding: 17,
    marginBottom: 20,
    height: 60,
    width: 150,
    marginLeft: 10,
    marginRight: 10,

    // shadowColor: '#006BFF',
    // shadowOffset: { width: 150, height: 150 },
    // shadowRadius: 200,
    // shadowOpacity: 1,

    shadowOffset: { width: 0, height: 1 },
    shadowColor: 'black',
    shadowOpacity: 0.3,
    elevation: 4,
    shadowRadius: 2,
    // background color must be set
    // backgroundColor: "#0000" // invisible color
    zIndex: 999,
    flexDirection: "row",
    justifyContent: "center",
}

export const buttonProfileSmallRed = {
    backgroundColor: '#9F847E',
    borderRadius: 8,
    padding: 17,
    marginBottom: 20,
    height: 60,
    width: 150,

    marginLeft: 10,
    marginRight: 10,

    // shadowColor: '#006BFF',
    // shadowOffset: { width: 150, height: 150 },
    // shadowRadius: 200,
    // shadowOpacity: 1,

    shadowOffset: { width: 0, height: 1 },
    shadowColor: 'black',
    shadowOpacity: 0.3,
    elevation: 4,
    shadowRadius: 2,
    // background color must be set
    // backgroundColor: "#0000" // invisible color
    zIndex: 999,
    flexDirection: "row",
    justifyContent: "center",
}

export const buttonProfileCenter = {
    backgroundColor: '#46A450',
    borderRadius: 8,
    padding: 17,
    marginBottom: 20,
    marginLeft: 20,
    marginRight: 20,

    // shadowColor: '#006BFF',
    // shadowOffset: { width: 150, height: 150 },
    // shadowRadius: 200,
    // shadowOpacity: 1,

    shadowOffset: { width: 0, height: 1 },
    shadowColor: 'black',
    shadowOpacity: 0.3,
    elevation: 4,
    shadowRadius: 2,
    // background color must be set
    // backgroundColor: "#0000" // invisible color
    zIndex: 999,
}

export const buttonLines = {
    padding: 17,
    marginBottom: 20,
    marginLeft: 20,
    marginRight: 20,
    borderColor: '#858585',
    borderTopWidth: 0.5,
    borderBottomWidth: 0.5,

    flexDirection: "row",
}

export const buttonArrow = {
    position: 'absolute',
    right: '6%',
    top: '68%',
}

export const buttonArrow2 = {
    position: 'absolute',
    right: '6%',
    top: '53%',
}

export const buttonArrow3 = {
    position: 'absolute',
    right: '3%',
    top: '20%',
}

export const buttonArrow4 = {
    position: 'absolute',
    right: '6%',
    top: '60%',
}
